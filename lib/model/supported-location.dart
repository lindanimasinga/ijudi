class SupportedLocation {
  final String name;
  final double latitude;
  final double longitude;
  final String region;

  SupportedLocation(this.name, this.region, this.latitude, this.longitude);
}
